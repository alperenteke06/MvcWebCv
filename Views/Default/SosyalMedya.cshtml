@using MvcWebCv.Models.Entity
@model List<TblSosyalMedya>

<div class="social-icons">
    @foreach (var x in Model)
    {
        <a class="social-icon" href="@x.Link"><i class="fab fa-@x.Icon"></i></a>
    }
</div>
